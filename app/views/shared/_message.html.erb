<% unless flash.empty? %>
  <script >
    <% flash.each do |key,val| %>
     <% type = key.to_s.gsub('alert','error').gsub('notice','success') %>
      toastr['<%= type %>']('<%= val %>')
    <% end %>
    
  </script>
<% end %>